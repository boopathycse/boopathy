#include <stdio.h>
#include <string.h>
 
int main()
{
   char a[100];
 
   printf("Enter a string to reverse\n");
   gets(a);
 
   strrev(a);
 
   printf("\nReverse of the string is %s", arr);
 
   return 0;
}
